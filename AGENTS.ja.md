> 英語原文: [AGENTS.md](AGENTS.md)

# メンテナー向け案内

PlainStormは、持ち運び可能な指示だけで構成されたスキルパッケージです。`skills/plainstorm/` にある採用済みの挙動を保ってください。ルートの選択は阻害要因に基づくままにし、状態管理と再開は任意のままにします。デバッグ、コードレビュー、すでに確定した実装は、このルートの対象外です。

- `skills/plainstorm/SKILL.md` と3つの参照文書を、挙動の根拠となる文書として扱います。説明用の文書で補足することはできますが、挙動をひそかに定義し直してはいけません。
- 変更していない採用済みの版をパッケージ化するときは、6つのスキルファイルをバイト単位でそのまま保ちます。更新をコミットする前にハッシュを確認してください。
- このリポジトリには、個人のパス、非公開の文脈、生のプロンプトや受領記録、実行時コード、プラグインのメタデータ、インストーラー、テレメトリー、CI、関係のないオーケストレーションを持ち込まないでください。
- ローカルコミットの前に、スキルのバリデーターを実行し、相対リンクと公開URLを確認し、個人パスを検索し、`git diff --check` を実行してください。

リポジトリの保守は、依頼者が示す作業・レビューの取り決めに従います。このスキルの利用に、その運用環境は必要ありません。また、このパッケージ自体がインストール、公開、デプロイの権限を与えることもありません。
